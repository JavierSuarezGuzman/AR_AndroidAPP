<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.DroidKot" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Sacar barra superior -->
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/noche</item>
        <item name="colorPrimaryVariant">@color/noche</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/mdr</item>
        <item name="colorSecondaryVariant">@color/mdr</item>
        <item name="colorOnSecondary">@color/gris</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
</resources>